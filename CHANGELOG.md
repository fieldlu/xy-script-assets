## 3.7.3.7 (2026-09-18)

### 修复
- **下载区「全选」范围限定为当前筛选结果**：原先全选只按文件名搜索词过滤、漏掉文件类型筛选，勾选 PDF 类型后点全选会把被隐藏的其它类型一并选中。现在全选只勾选当前可见且符合当前类型的条目；切换类型或修改搜索词后再次全选，范围随之收敛为新的可见结果，不再残留被过滤掉的旧勾选。
- **勾选状态一致性**：全选、单个勾选、清空与批量下载队列共用同一套 ID 归一化与可见性判定，勾选集 ↔ 列表复选框回显 ↔ 下载队列三者保持同步。

### 其他
- 脚本头部 `@description` 措辞合规化改写，功能描述与实际能力对齐。

---
## 3.7.3.6 (2026-09-16)

### 修复
- **网络请求统一超时保护**：全脚本请求收编为带超时的统一封装（普通接口 15s、整卷数据/图片 30-45s），弱网或服务端无响应时不再永久卡住；持久定时器增加单 tick 硬超时，悬挂自动让出并保留串行语义。
- **修复「教师设置观看时长仍按默认时长执行」**：雷达缓存解包口径修正；进任务后的时长同步改为持续重试直到生效，跨任务时丢弃过期结果，不再写脏新任务的交卷线。
- **看门狗喂狗条件修正**：修复「开着学习记录时防死锁刷新永不触发」；视频元素缺失加约 2 分钟宽限窗口，既不误刷新也保留自愈能力。
- **修复全局雷达任务键类型不一致**：Map 写入与读取统一字符串化，修复「添加到队列」「完成确认选中任务」静默失效。
- **补回跳转重试计数跨页持久化**（对齐 3.7.3.5 的跨页累计能力）：计数写入 `xy_jump_retry_key` / `xy_jump_retry_count`，整页导航后不再归零。
- **批量完成确认**异常时按钮不再永久禁用（补 finally 恢复）；复制失败不再误报「已复制」。
- **文件 URL 解密失败**不再把密文当直链使用：解密失败时合法直链透传、真密文跳过该项。
- **面板控件绑定加空值兜底**：单个控件缺失时跳过绑定并打日志，不再让整块面板半绑定且无法自愈。

### 安全
- **输出转义加固**：转义函数补齐引号转义，修复课程名、任务 ID 等字段直插 innerHTML 属性上下文的注入风险（20 处汇聚点）。

---
## 3.7.3.5 (2026-09-14)

### 修复
- **有些连播不显示当前任务**：调度状态的「残留清理」判定原先依赖 `sessionStorage` 的标签页身份——连播跳转若发生在新标签页、复制链接打开或标签页恢复，`sessionStorage` 是全新的，会被误判为新会话并清空正在运行的队列，主面板卡片随即隐藏（`updateSchCard` 首行按 `isRunning` 判定）。现改为**心跳时间戳判定**：新增 `xy_schedule_heartbeat`，连播每 tick 打点，启动时只要距上次心跳在 5 分钟（`XY_SCH_HEARTBEAT_TTL`）内即视为同一场连播续跑，保留现场；超过 5 分钟才判定为残留并清理。跨标签页、复制链接、标签页恢复三种场景均正确。
- **标签页冻结后切回被误判为残留**：TTL 若取 60 秒，浏览器后台节流或系统休眠会让主循环停摆超过 60 秒，此时切回并刷新会把仍在运行的连播误清空——正是本版要修的症状。除把 TTL 放宽到 5 分钟外，另增加唤醒补打点：监听 `visibilitychange` / `focus` / `pageshow`，页面重新可见时若调度仍在运行则立即续期心跳。
- **跳转失败无限重跳**：SPA 路由拦截或目标页异常时，兜底逻辑会「跳过 → 复位 → 再跳」，缺少重试上限，可能持续循环消耗。现按任务维度（`group_id:node_id`）计数，连续失败 3 次后暂停调度并提示手动检查任务链接。计数持久化在 `xy_jump_retry_key` / `xy_jump_retry_count`，整页导航后仍能跨页累计，避免「导航成功但落在错误节点」（目标下架 / 无权限被踢回首页）绕开上限；用户主动跳过 / 停止 / 重新开始时统一清空。
- **暂停态不清心跳**：暂停超过 TTL 后刷新会丢失现场。现暂停时清除心跳、继续时补回，语义与「暂停后长时间不恢复按残留清理」一致。
- **启动路径未补心跳**：停止 / 暂停会清心跳，若四条启动路径（智能排课导入后启动、卡片启动按钮、`xySchStart`、`xySchRestart`）不补回，「停止 → 重启 → 立刻刷新」会被判定为残留并清空刚恢复的队列。现统一在启动点强制续期。
- **心跳写入放大**：心跳原先每 tick（1 秒）写一次 GM 存储，长时挂机产生大量冗余写入。现节流为 10 秒一次（`force` 参数可穿透，供启动与唤醒补点使用），60 秒内写入由 60 次降到 6 次。
- **跳转态卡片不显示进度**：跳转进行中的模板只有任务名，没有序号，与其它状态不一致。现改为「🚀 正在跳转 · 第 N/M 项 → 「任务名」」。
- **`isJumping` 不复位**：该标志只在手动跳过时复位，若跳转失败（目标页异常、导航被拦截）会永久停在 true，卡片卡在「正在跳转」不再更新。现增加 15 秒兜底复位，超时后自动恢复调度接管。
- **任务重复入队**：雷达连播与智能排课此前各自过滤待办列表，重复条目与无效条目可能被重复入队。现在统一由 `xyBrushFilterPlayable` 做硬性过滤与去重（`task_type===1`、未完成、`node_id` 存在，按 `group_id:node_id:task_id` 去重），三个入口共用；时间窗口与文件名后缀等**额外条件仍由各入口自行追加**，能力与替换前完全等价。
- **排序规则**：`xyBrushTaskSort` 恢复为「截止时间 → 完成状态 → 分组（数值比较）→ 节点」，修正分组用字符串字典序比较（`'100' < '99'` 成立）与丢失完成状态分段的问题。

### 优化
- **连播基础口径统一**：一键连播、雷达连播、智能排课三个入口此前各写一套筛选逻辑，同一批任务在不同入口下的去重与脏数据过滤结果不一致，现在共用同一份基础筛选结果。
- **心跳请求加固**：学习时长心跳的 `learnRecord` 与用户信息请求加入 `xyHeartbeatFetch`（30 秒超时 + AbortController），不再可能因单次请求挂死而阻塞后续重试；重试退避在指数间隔上叠加随机抖动，避免多标签页同时重试造成请求尖峰；心跳响应新增 HTTP 状态码判断，非 2xx 直接计入重试而非继续解析。
- 新增 `xyScheduleHeartbeatClear`，在强停、手动停止、全部完成三处清除心跳，避免残留心跳让下一场连播被误判为上一次的续跑。

---

## 3.7.3.4 (2026-09-10)

### 新增
- **脚本头部协议与第三方声明**：脚本源码头部新增「开源协议与第三方声明」注释块，注明 GPL-3.0 协议、引用的四个开源项目（小雅爬爬爬 / 小雅做做做 · zygame1314 · MIT；小雅自动刷 / 小雅粘粘粘 · Qy · Apache-2.0）的致谢与归属，并链接仓库内的 LICENSE、LICENSE-MIT、LICENSE-APACHE 与 THIRD_PARTY_NOTICES.md；元数据新增 `@source` 指向仓库地址。功能代码零改动。

---

## 3.7.3.3 (2026-09-08)

### 新增
- **任务名点击跳转**：「任务与作业明细」与「今日学习提示」中的任务条目现在可以直接点击进入对应任务页。此前待办接口不返回父章节 ID，所有条目都被渲染为不可点击；现在缺失的父章节 ID 会从课程资源树按节点自动反查补齐。

### 修复
- **时长任务提前放行**：教师为资源设置最低观看时长（`watch_min_minutes`）时，文档引擎的提交线与放行线此前写死为 2 分 10 秒 / 5 分钟，导致未满时长的确认请求被服务端拒绝、本地却按超时跳走，任务始终无法在后台确认完成。现在进入任务时会读取该任务的时长要求，提交线动态对齐为「要求分钟 + 10 秒缓冲」，放行线同步顺延；未设置时长的任务行为不变。

### 优化
- **调度时长自动规划**：智能排课、一键雷达连播、任务库添加三个入队入口不再一律按 30 分钟规划，改为自动读取任务的时长要求。
- **调度悬浮卡信息精简**：运行中条目只显示序号、任务名与已驻留时间，不再显示达标状态与所需分钟数。达标判定逻辑不变：视频与文档分别按各自标准由服务端确认后跳转，与显示的驻留时间无关。
- **交互细节**：可点击任务行悬停时增加主题色指示条与过渡，「进入 →」箭头增加微动效，并补充键盘焦点描边。

---

## 3.7.3.2 (2026-08-31)

### 新增
- **统一导出面板**：「导出作答文档」与「手写归档」合并为一个入口，支持普通导出（打印体排版）与笔记版导出（手写笔迹纸张）两种模式自由切换。
- **内容范围自由组合**：题目 / 标准答案 / 我的作答三项可勾选，一键导出「仅题目」「题目+标准答案」「仅我的作答」等组合；导出前提供实时预览（前 2 页），所见即所得。
- **配置记忆**：导出模式、内容范围、格式、字号即改即存，下次打开保持上次选择。
- **组合题（大题套小题）识别**：`1.1 / 1.2` 形态的组合题按子题拍平为独立条目，每道子题旁带大题干与题图；AI 提示词与作答提交均支持 `1.1 => A` 子题编号。

### 修复
- **笔记版图片丢失**：统一图片管线——题目图、选项图、手写上传附件图在所有导出格式（Word/PDF/图片/笔记版 PDF·JPG）中完整嵌入；此前笔记版/PDF/图片格式除 docx 外均无法出图。
- **手写上传图不识别**：附件题作答的 dist 附件（JSON 数组形态）此前被降级为纯文本，现已正确解析为图片并嵌入。
- **长文本溢出页边**：修复换行判定缺陷，长题干/长选项不再冲出页面右缘被裁剪。
- **行重叠**：修复文本块行距推进缺陷，行与行不再互相重叠。
- **组合题大题干图重复/丢失**：拍平后每道子题完整携带大题干段（含题图），不再出现 `[图片]` 死占位。

### 优化
- **版式统一**：笔记版手写纸统一为 A4 空白横格，移除大作业纸版式；导出面板四分区（导出模式/内容范围/格式与字体/预览）更清晰。
- **字号可选**：普通导出支持五号/小四/四号三档字号。

---

## 3.7.3.1 (2026-08-29)

### 优化
- **内部清理**：移除 15 个废弃函数（反馈旧问卷/Google 表单配置、鼠标模拟手动开关、深度伪装手动开关、看门狗停止器、重复的 HTML 转义工具等），脚本更轻更稳。
- **功能零变化**：雷达连播、安全循环、防休眠、保活、鼠标模拟、深度伪装、作业、手写归档全部引擎照常运行。

---

## 3.7.3.0 (2026-08-28)

### 新增
- **作业手写归档**：作业题目/我的作答一键生成手写版，导出 .hinote/PDF/JPG，交手写作业再也不用手抄了！

### 修复
- **文件预览确认弹窗卡住**：识别 Ant Design 内嵌确认块，精准点「确定」，挂机流程不再被拦截。
- **非选课课程误显学情**：路由判定修正，不再渲染全零的学情数据。
- **作业区数据识别失效**：全面适配新版作业接口，浏览页与答题页题目、作答记录、批改状态全识别。

### 优化
- **面板大瘦身**：把那些用不到的开关全藏起来，只留「强制静音」一个开关，引擎默认全开省心。
- **作业自动拉取 10 秒冷却**：防止疯狂请求把号封了。
- **功能零删减**：雷达连播、安全循环、手动休眠、防休眠、保活、鼠标模拟、深度伪装等引擎全部保留并默认运行。

---

## 3.7.2.7 (2026-08-28)

### 美化
- **美化了一处**

---

## 3.7.2.5 (2026-08-25)

### 新增
- **🎬 视频直链下载**：VOD 课程视频经 play_auth → Aliplayer 换流解析公网 mp4 直链，支持跨域媒体下载通道直接保存
- **📐 浮窗自由缩放**：主控台浮窗八方向边缘/角落热区拖拽调整宽高，右下角斜纹 grip 可视手柄；宽高持久化记忆，双击 grip 一键复位默认尺寸

### 修复
- **收起残留空白**：最小化时挂起自定义高度仅保留标题栏，展开时恢复原尺寸
- **学情概览压扁**：概览视图禁止收缩并保底 `min-height: 220px`，内容区内部滚动
- **面板滚动**：所有分区统一保持主内容区可滚动，小尺寸面板下方内容不再被裁切

### 优化
- **视图组件化**：面板拆分为纯模板 / 编排器 / 事件装配三段式架构
- **系统控制条瘦身**：两行压缩为单行紧凑工具条（约 95px → 39px），完整功能名移至 hover 提示
- **滚轮滚动接力**：移除概览内容区 `overscroll-behavior: contain` 阻断，悬停面板任意位置均可顺畅滚动、内层到头无缝接力外层；外层加 `contain` 防止穿透到背后网页

---

## 3.7.2.4 (2026-08-24)

### 新增
- **🎬 视频直链下载**：VOD 课程视频经 play_auth → Aliplayer 换流解析公网 mp4 直链，支持跨域媒体下载通道直接保存
- **📐 浮窗自由缩放**：主控台浮窗八方向边缘/角落热区拖拽调整宽高，右下角斜纹 grip 可视手柄；宽高持久化记忆，双击 grip 一键复位默认尺寸

### 优化
- **视图组件化**：面板拆分为纯模板（xyBuildPanelTemplate）/ 编排器（createUI）/ 事件装配（xyBindPanelEvents）三段式架构

---

## 3.7.2.3 (2026-08-22)

### 新增
- **统一学情概览**：课程内 `/task`、`/home`、`/courseTools` 及裸 `/mycourse/{id}` 一律进入学情概览，不再有独立任务页/工作台
- **🛰️ 小雅引擎**：原「待命区」更名为「小雅引擎」，语义更贴合实际功能

### 优化
- **概览秒开**：进入学情概览时使用缓存即时渲染，不再白屏或闪回原区
- **静默切换**：区域切换日志全部静默，不再弹出「底层指令」toast 刷屏

### 修复
- **移除任务页残留**：删除整个任务页 zone 及配套加载/渲染/跳转代码，消除与学情概览的重复与冲突

---

## 3.7.2.2 (2026-08-22)

### 修复
- **学情概览切换竞态**：修复旧页面扫描与用户打开学情概览并发时，反复输出「已切换至进行中课程总览 / 课程学习数据概览」并回切页面的问题
- **中央分区仲裁**：固定打开的学情概览会阻断旧扫描的自动分区切换；右上角显式返回会先释放固定状态，仍可正常回到原分区

### 测试
- **概览仲裁回归**：覆盖“旧扫描不可覆盖固定概览”与“显式返回可恢复课程总览”两条路径

---

## 3.7.2.1 (2026-08-22)

### 新增
- **今日学习提示**：课程总览提供全局行动建议，单课程学情提供本课优先事项；建议基于待办状态、截止时间和完成进度排序，并可直达原任务

### 优化
- **课程工作台交互**：课程总览与课程学情可稳定往返，任务明细的展开状态会保留，滚动保持单一纵向通道

### 修复
- **异步状态保护**：概览打开时不会再被课程扫描、讨论捕获或作业处理事件覆盖；同课程切换作业时旧响应不再覆盖当前作业
- **下载与 SPA 生命周期**：下载前拦截失效鉴权并净化文件名；页面重建会清理旧的文档级拖拽监听器

---

## 3.7.2 (2026-08-21)

### 新增
- **课程学习数据概览**：作为主控台正式区域展示学习时长、任务完成度与作业成绩；任务与作业明细可展开，并可直达原任务

### 优化
- **概览阅读体验**：统一为单一纵向滚动通道，重构学习时长与任务完成度的视觉层级

### 修复
- **概览路由稳定性**：修复路由轮询在课程总览与学情概览之间重复切换，避免重复输出「底层指令」
- **情报站横向溢出**：超长公告链接自动换行，且内容区允许收缩，不再撑宽控制台

---

## 3.7.1 (2026-08-21)

### 优化
- **待办课程排序**：依次展示有可做待办、无可做但有已截止任务、其余课程；每组按截止时间从早到晚、总任务数从多到少排序
- **作业保存反馈**：全部题目保存成功后提示并自动刷新页面；存在失败或跳过题目时保留当前结果与错误信息

### 修复
- **作业 Record ID 兼容**：优先复用题目数据中的作答记录，并遍历任务流程兼容字段，修复部分 `course_paper` 页面初始化提交参数失败

---

## 3.7.0 (2026-08-21)

### 新增
- **进行中课程总览**：在「我的课程」页汇总进行中课程的当前待办、整体完成率与累计学习时长，支持按课程名搜索和按待办筛选
- **课程学情抽屉**：集中展示单门课的学习时长、任务完成度和作业得分；作业条目可直接进入对应详情页
- **任务状态语义**：作业明确区分待提交、已截止、待批阅和已批阅，课程卡片显示「N 项可做」「N 项已截止」「任务已完成」或「暂无任务」

### 优化
- **待办计算**：严格以 `un_finish` 任务的截止时间划分可做与已截止，避免跨接口相减造成误判
- **空任务展示**：`0/0` 显示「暂无任务」，不再渲染误导性的 100% 进度条
- **更新来源**：脚本检查更新与 Tampermonkey 自动更新统一使用 Gitee 发布源

---

## 3.6.4 (2026-08-17)

### 修复
- **下载按钮无响应**：修复事件委托只识别按钮本身、资源 ID 映射不完整导致点击后没有任何反馈的问题
- **课程资源结构兼容**：兼容数组/对象两种课程资源返回结构，补齐 quote_id 与嵌套资源识别
- **恢复参考脚本下载链路**：下载文件时继续携带站点 Authorization，并使用流式读取后生成 Blob
- **批量下载进度**：按文件流逐块显示当前进度，修复下载过程中长期显示 `0/1 (0%)`，并确保成功/失败都能推进批量计数
- **未知总大小提示**：文件服务器未返回 `Content-Length` 时显示当前文件名和已接收字节数；可从 `Content-Range` 推断总大小
- **下载进度界面紧凑化**：批量进度、文件名、文件百分比、接收量和操作按钮分层显示，长文件名自动省略，避免文字溢出下载面板
- **单文件下载进度**：点击单个文件的下载按钮时复用同一进度卡片，显示 `0/1`、当前文件百分比、已接收大小和完成状态
- **下载区课程切换竞态**：修复 A → B → A 场景下旧请求覆盖当前课程资源、目录树和排序信息的问题
- **下载资源失败重试**：请求异常不再被误判为空资源，失败后可自动重试或手动刷新
- **动态内容安全**：统一转义课程目录、文件名、提示信息等动态 HTML 内容
- **下载资源 ID**：统一规范化资源 ID，修复数字/字符串 ID 在批量下载、目录树和单文件下载之间不一致的问题

### 优化
- 优化 MutationObserver、持久定时器和页面可见性恢复逻辑
- 补充下载区、课程扫描和刷新入口的 Promise 异常处理

---
## 3.6.3 (2026-08-11)

### 新增
- **脚本更新模块**：面板头部「检查更新」，一键比对当前/最新版本
- **更新面板**：显示当前版本、最新版本、更新内容与发布页入口
- **自动检查**：脚本每 6 小时静默检查一次，发现新版本弹窗提示（每版仅提醒一次）

### 优化
- **面板头部改版**：第二行改为分段操作条（当前区域 / 反馈 / QQ群 / 检查更新），移除杂乱的彩色胶囊徽章
- 元数据新增 `@downloadURL`，启用 Tampermonkey 原生自动更新
- 开源协议改为 GPL-3.0

---

## 3.6.2 (2026-08-02)

### 新增
- **调度任务库对齐雷达树状**：课程 → 单元 → 任务 三级展示，单元可折叠
- **显示全部学生课程**：调度/雷达不再只显有待办任务的课，全完成的课也可重新挂机

### 优化
- 默认关闭开机动画（xy_no_splash），启动更快
- 任务库/雷达共用同一套目录树分组逻辑，四处树状结构统一

### 修复
- 目录跳转 URL（父目录id/任务id）、任务识别（is_task / resource_type / 嵌套 task_type）

---

## 3.6.1 (2026-08-02)

### 新增
- **课程目录区**：课程目录页自动展示树状目录，点击任意项跳转对应资源
- **下载区单元顺序树状呈现**：按平台目录结构展示，与左侧目录一致
- **下载区类型筛选**：按文件类型勾选过滤，默认全选
- **全局雷达树状**：课程 → 单元 → 任务 三级树状展示
- **自动破除系统弹窗**：刷课区/下载区自动清理平台弹窗

### 优化
- 下载按钮紧贴彩色类型标签
- 目录排序对齐平台左侧目录（sort_position + 解包 root）
- 目录页识别兼容无节点数字/纯课程首页

### 修复
- 课程目录树与平台结构不一致（root 包裹、乱序）

---

## 3.6.0 (2026-08-02)

### 新增
- **作业答题台**：全新「作答 / 结果」双 Tab 操作台，支持提取题目模板、粘贴 AI 回答、一键提交作答记录（逐题提交，提交后自动刷新成绩）
- **提交结果面板**：成绩摘要（大数字 + 正确/错误/部分/待批统计）+ 筛选（全部/错题/待批）+ 每题卡片明细（状态徽章、我的答案、标准答案）
- **下载区文件徽章**：文件类型改用彩色扩展名标签（PDF/DOC/PPT/XLS/ZIP/MP4 等），文字可辨，色弱色盲友好

### 优化
- **下载区排序**：支持按单元顺序 / 文件名 / 上传时间排序，单元顺序按平台 sort_position 逐层排序，与网页端一致
- **下载区横向滚动**：长文件名可左右滑动完整查看
- 移除脚本内全部注释，精简体积

### 修复
- 结果面板长答案强制换行，不再出现横向滚动条
- AI 作答模板不再出现 undefined（题型标签字段对齐数据模型）

## 3.6.1 (2026-08-02)

### 新增
- **课程目录区**：课程目录页自动展示树状目录，点击任意项跳转对应资源
- **下载区单元顺序树状呈现**：按平台目录结构展示，与左侧目录一致
- **下载区类型筛选**：按文件类型勾选过滤，默认全选
- **全局雷达树状**：课程 → 单元 → 任务 三级树状展示
- **自动破除系统弹窗**：刷课区/下载区自动清理平台弹窗

### 优化
- 下载按钮紧贴彩色类型标签
- 目录排序对齐平台左侧目录（sort_position + 解包 root）
- 目录页识别兼容无节点数字/纯课程首页

### 修复
- 课程目录树与平台结构不一致（root 包裹、乱序）

---

## 3.6.0 (2026-08-02)

### 新增
- **作业答题台**：全新「作答 / 结果」双 Tab 操作台，支持提取题目模板、粘贴 AI 回答、一键提交作答记录（逐题提交，提交后自动刷新成绩）
- **提交结果面板**：成绩摘要（大数字 + 正确/错误/部分/待批统计）+ 筛选（全部/错题/待批）+ 每题卡片明细（状态徽章、我的答案、标准答案）
- **下载区文件徽章**：文件类型改用彩色扩展名标签（PDF/DOC/PPT/XLS/ZIP/MP4 等），文字可辨，色弱色盲友好

### 优化
- **下载区排序**：支持按单元顺序 / 文件名 / 上传时间排序，单元顺序按平台 sort_position 逐层排序，与网页端一致
- **下载区横向滚动**：长文件名可左右滑动完整查看
- 移除脚本内全部注释，精简体积

### 修复
- 结果面板长答案强制换行，不再出现横向滚动条
- AI 作答模板不再出现 undefined（题型标签字段对齐数据模型）

## 3.6.0 (2026-08-02)

### 新增
- **作业答题台**：全新「作答 / 结果」双 Tab 操作台，支持提取题目模板、粘贴 AI 回答、一键提交作答记录（逐题提交，提交后自动刷新成绩）
- **提交结果面板**：成绩摘要（大数字 + 正确/错误/部分/待批统计）+ 筛选（全部/错题/待批）+ 每题卡片明细（状态徽章、我的答案、标准答案）
- **下载区文件徽章**：文件类型改用彩色扩展名标签（PDF/DOC/PPT/XLS/ZIP/MP4 等），文字可辨，色弱色盲友好

### 优化
- **下载区排序**：支持按单元顺序 / 文件名 / 上传时间排序，单元顺序按平台 sort_position 逐层排序，与网页端一致
- **下载区横向滚动**：长文件名可左右滑动完整查看
- 移除脚本内全部注释，精简体积

### 修复
- 结果面板长答案强制换行，不再出现横向滚动条
- AI 作答模板不再出现 undefined（题型标签字段对齐数据模型）

## 3.5.7 (2026-07-22)

### 新增
- **反馈入口**：控制面板新增「💬 反馈」链接，点击直接跳转 ScriptCat 创建 Issue 页面
- 支持通过控制台  导出本地暂存的反馈数据

### 优化
- 移除未使用的 CDN 依赖（SurveyJS），精简脚本体积
- 清理反馈问卷相关冗余代码

# Changelog

## 3.5.6 (2026-06-17)

### 修复
- **作业题目自动解析**：进入题目页面无需刷新即可识别，支持 `course_paper` 路径类型的 URL（`/course_paper/mycourse/{gid}/{pid}/...`）
- **课程中心双面板**：新增 `@noframes` 防止 iframe 注入，修复页面出现两个脚本面板的问题
- **SPA 面板丢失**：修复 `MutationObserver` + `requestAnimationFrame` 竞争条件导致面板创建失败的问题
- **课件下载权限**：可访问但未加入的课程也能下载课件（处理 API `code: 50007`，自动获取访问令牌）

### 优化
- 作业题目主动拉取增加重试机制（最多 6 次指数退避）
- 面板创建增加互斥锁和残留清理
- 描述与许可文本适配平台审核要求

## 3.5.5 (2026-06-15)

### 新增
- **作业题目导出**：支持将作业/测验中的题目导出为 Word 文档（.docx），包含题目文字与图片

### 优化
- 脚本整体版本维护更新，3.5.4 各项功能持续稳定

## 3.5.4 (2026-06-12)

### 新增
- **一键雷达连播**：休眠区新增一键连播按钮，自动扫描全网未完成任务，筛选视频/文档类，按 DDL 截止时间智能排序，一键导入调度队列并立即启动连播
- **刷课区雷达连播对齐**：课程区"雷达连播"模式按钮与休眠区一键连播使用统一逻辑

### 修复
- **情报站本地化**：公告不再依赖跨域 CORS 代理，改为嵌入式本地数据，彻底解决公告无法加载问题，实现秒开零延迟

## 3.5.3 (2026-05-18)

### 精简
- **开关面板精简**：隐藏防休眠/后台保活/鼠标模拟/深度伪装四个开关（默认开启），其余保持可见

## 3.5.2 (2026-05-18)

### 修复
- **视频断点续播**：移除强制 `currentTime=0` 逻辑，视频从小雅平台记录的断点继续播放，不再每次从头开始
- **拖动检测优化**：检测到进度条拖动时弹回原位而非刷新整个页面，避免打断连播流程

## 3.5.0 (2026-05-10) — 3.4 系列结档

### 新增
- **课程页调度进度卡片**：模式按钮下方独立区域，实时显示任务进度和快捷操作
- **关闭浏览器清空调度**：刷新保留，关浏览器自动清空
- **后台防节流振荡器**：静默音频保持页面活跃，切后台定时器不降频
- 调度卡片 addEventListener 替代 onclick，Tampermonkey 沙箱兼容

### 修复
- **致命** 调度跳转自动重置：sessionStorage 会话标记替代心跳方案
- **致命** 自动定时启停依赖 overlay DOM → 改用全局函数
- **致命** 模式切换停止调度依赖 overlay DOM → 改用全局函数
- **致命** JSON.parse / parseInt / sessionStorage 无保护 → 全量 try-catch
- **中** duration ≤ 0 导致瞬间完成 → 迁移时强制最低 30min
- **中** task.name 无保护 → 全量加 `||'未知'`
- **中** overlay 启动时序错误 → updateCourseUI 移到 isRunning=true 之后
- **中** overlay 启动缺防节流振荡器 → 补齐
- **低** 暂停时状态栏仍显示运行中 → 区分"运行中"与"已暂停"
- **低** 日夜间模式文字颜色不跟随 → updateSchCard 每次刷新 color

### 架构
- 5 个全局调度函数 `xySchStart/Pause/Stop/Skip/Restart` 统一卡片和 overlay
- 调度卡片独立渲染 + addEventListener，按钮不依赖 onclick 沙箱跨域
- 防后台节流：maxCatchUp 30→300 + visibilitychange 强制刷新卡片

## 3.4.8 (2026-05-09)

### 修复
- **致命** 调度跳转自动重置：页面跳转后 `isRunning` 被初始化逻辑误杀，调度系统秒停 → 新增 `sessionStorage` 跳转标记，调度主动跳转时跳过重置
- 调度完成/停止后同步重置 `isPaused` 标志

### 新增
- **调度暂停/继续按钮**：运行中可随时暂停（保留队列进度），点击继续无缝接上
- `isPaused` 状态持久化到 `GM_setValue`，页面刷新不丢失
- 暂停按钮自动显隐：未运行时隐藏，运行中显示并反映当前暂停态

### 优化
- 调度控制按钮统一 `updateSchButtons()` 管理，避免各处手动改状态不一致

## 3.4.7 (2026-05-09)

### 修复
- 移除 `@grant GM_xmlhttpRequest`，公告获取改用 CORS 代理方案，安装时不再弹出跨域授权提示
- 双代理自动容错：主代理 (`allorigins.win`) 挂了自动切换备代理 (`codetabs.com`)
- 公告渲染增加 `escapeHtml` 防护，杜绝 JSON 注入 XSS 风险

### 优化
- `fetchCloudIntelligence` 从 60 行双轨代码精简为 30 行纯 `fetch` 递归容错

## 3.4.6 (2026-05-08)

### 修复
- **致命** `injectDuration` 调用未定义的 `_origSendRecordRequest` → 时长注入功能完全不可用 → 已修复
- **中** 看门狗重建定时器用原始 `setInterval` → 浏览器可节流 + `.clear()` 类型错误 → 已修复
- **中** 注入循环嵌套重试无效 → `sendRecordRequest` 从不 throw，失败包仍计为成功 → 已修复
- **低** `injectDuration` 无上限 → 控制台可恶意发包 → 已加 300 分钟上限
- **低** 注入未检查 `activeZone` → 与正常心跳防护不一致 → 已补齐

### 加强
- 注入期间占用 `isRecordSending` 锁，阻止正常心跳并发重复发包
- `_origSendRecordRequest` 重构为独立核心发包函数，`sendRecordRequest` 薄包装复用
- 注入清理逻辑用 `try-finally` 保证锁和状态在任何退出路径下正确释放
- 新增 `inject-test.js` 独立测试脚本，支持 `--dry` 干跑和 `--verbose` 详细模式

## 3.4.5 (2026-05-08)

### 修复
- **下载引擎全面修复**：`downloadFile` 携带 `Authorization` Token，修复云端文件拒绝访问
- **加密链接解密**：新增 `decryptFileUrl` 函数，支持 DES 解密加密的文件 URL
- `getDownloadUrl` 增加 `is_encryption` 检测，自动解密加密链接

### 新增
- **下载区搜索框**：实时过滤文件名，全选智能适配搜索结果
- **下载区进度条**：批量下载实时显示百分比，绿紫渐变可视化
- **下载区暂停/终止**：随时暂停继续，一键终止（AbortController 中断 fetch）
- 文件计数栏动态显示 `"N 个文件 (已过滤)"`

## 3.4.4 (2026-05-07)

### 新增
- **持久化定时器引擎**：7 个关键定时器全部升级为防后台节流版本
- 浏览器切后台/最小化后切回前台，自动补偿错过的计时和心跳
- `visibilitychange` 事件监听，前台恢复触发即时补偿
- `createPersistentInterval` 通用包装函数，250ms 自检 + 自动补帧

### 优化
- 实时计时器 (`realTimeTimer`) 后台恢复后自动补齐落下的秒数
- 30s 学习心跳 (`recordIntervalTimer`) 后台恢复后补齐心跳包
- 1s 主循环、5s 跳课引擎、3s DOM 扫描、1s 调度器全部持久化

### 其他
- README 新增隐私协议与免责声明
- notice_new.json 新增隐私声明与免责条款

## 3.4.3 (2026-05-07)

### 新增
- **赛博朋克二次元启动画面**：冷启动专属 HUD 动画（星空、光环、菱形粒子、六边形、扫描线、脉冲波），刷新跳过
- **智能跳转优化**：同课程按 node_id 顺序跳、跨课程优先清完一门再下一门
- **雷达 API 缓存去重**：3 秒 TTL + 并发请求合并，大幅减少重复 API 调用
- **跳转失败指数退避**：无任务 5s→10s→20s→40s→80s→10min，网络异常独立退避
- **学时注入三重防护**：面板默认折叠 + 二次确认弹窗 + 数字匹配验证；默认值清空必须手动输入
- **开源协议更新**：禁止修改、复制、售卖、代刷

## 3.4.1 (2026-05-05)

### 修复
- **CRITICAL** `triggerDocBatchSniper()` 引用未定义 `token` → 文档批量清理功能永久失效 → 已修复
- **CRITICAL** `autoLikeAction()` btn null 引用 → 点赞按钮缺失直接崩溃 → 已添加 null 守卫
- **CRITICAL** `fetchGlobalTasks()` 空 catch 静默吞噬所有错误 → 已添加日志
- **HIGH** `escapeHtml` 误用于正则转义 → 输入特殊字符直接崩溃 → 已新增 `escapeRegex()`
- **HIGH** 键盘模拟 `keyCode` 短路 bug → 全部发送错误 keyCode → 已修复为映射表
- **HIGH** 保活看门狗时间戳恒更新 → 看门狗完全失效 → 改为仅在真实成功后更新
- 修复情报站 404：重建 notice_new.json
- 新增 @updateURL / @downloadURL 元数据，支持油猴自动更新检测

### 优化
- **学时注入**：红色警告面板 + 二次确认弹窗 + 数字验证 + 默认折叠 + 清空默认值
- **QQ 群**：面板快捷复制入口
- **成就系统**：退役移除，界面更清爽
- 移除开发草稿注释和重复分隔线

## 3.4.0 (2026-05-04)

### 新增
- **后台保活引擎**：看门狗每 10s 巡检，心跳缺口超 75s 自动补发；检测到定时器被 GC/节流回收时自动重建
- **手动时长注入**：UI 面板输入分钟数 → 自动拆分为心跳包序列发送（每包 = 30s），支持随时中断
- **注入进度条**：实时显示 已完成/总数（百分比）
- 控制台快捷命令：`xyInjectDuration()` / `xyStopInject()` / `xyKeepaliveStatus()`

### 优化
- 注入函数复用核心心跳通道，不重复实现签名/Token 逻辑
- 保活看门狗与连播/调度/秒交完全解耦

## 3.3.0

- UI 全面美化，紧凑可折叠面板

## 3.2.0

- 修复学习记录上报多项缺陷，增强健壮性
